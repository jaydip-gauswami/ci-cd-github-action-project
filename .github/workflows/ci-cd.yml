name: CI-CD

on:
  push:
    branches: [ main ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install --prefix app

      - name: Run tests
        run: npm test --prefix app

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ jaydipgiri }}
          password: ${{ dckr_pat_xCGayzT9K5FS-6Q_9hTfPT30m-E }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ secrets.jaydipgiri }}/demo-app:latest
